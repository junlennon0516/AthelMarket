# 에셀 마켓 사이트 실행 방법

## 메인 앱 (`frontend/react`)

터미널에서 아래 순서대로 실행합니다.

```bash
cd frontend/react
npm install
npm run dev
```

브라우저에서 **http://localhost:5173/** 로 접속합니다.

### 기타 스크립트

| 명령 | 설명 |
|------|------|
| `npm run build` | 프로덕션용 빌드 |
| `npm run preview` | 빌드 결과 미리보기 |
| `npm run lint` | ESLint 검사 |

---

## 참고: 루트의 `react/` 폴더

구조가 비슷한 예전/별도 앱입니다. 실행하려면:

```bash
cd react
npm install
npm run dev
```

일반적으로는 **`frontend/react`** 를 사용하면 됩니다.
